const asyncHandler = require("express-async-handler");
const Product = require("../model/Product");

const getProductsForSeller = asyncHandler(async (req, res) => {});

module.exports = { getProductsForSeller };
